apiVersion: v1
kind: Service
metadata:
  name: trading-intelligence-agent
  labels:
    app: trading-intelligence-agent
spec:
  selector:
    app: trading-intelligence-agent
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
  type: ClusterIP
